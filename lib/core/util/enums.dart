enum ThemeOption { system, materialYou, darkAmoled, custom }
enum CompilePlatform { windows, linux, macos, web, ios, android }
enum CompileMode { debug, profile, release }
enum CompileArch { arm, arm64, x32, x64 }
